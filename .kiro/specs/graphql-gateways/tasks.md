# Tasks: GraphQL Gateways

1. Author HyperGraphQL templates [config + schema] targeting `SPARQL_ENDPOINT` (GraphDB/Fuseki).
2. Provide GraphQL‑LD/Comunica example (minimal Node server + JSON‑LD context) and docs.
3. Write gateway README with quickstart, env configuration, and troubleshooting.
4. (Optional) Add Docker Compose for HyperGraphQL (external SPARQL endpoint via env).
5. Validate against local sample data and document example queries.


