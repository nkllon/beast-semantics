# Tasks: Ontology Core Platform

1. Document canonical env/config schema and provide `.env.sample`.
2. Codify Cloudflare baseline controls (WAF, Access, rate limits) in a shared runbook.
3. Provide shared templates: HyperGraphQL schema seed and GraphQL‑LD context seed.
4. Define cache key strategy and persisted query conventions; provide examples.
5. Provide load-test harness templates (k6 or Artillery) and a results template.
6. Standardize dashboards/alerts (metrics list, thresholds) and logging fields.
7. Document release workflow conventions and artifact layout; ensure both specs reference them.


