# Tasks: Ontology Publishing

1. Extend build to emit JSON-LD contexts and pyLODE HTML docs.
2. Generate VoID/DCAT metadata from build inputs and outputs.
3. Implement Cloudflare Worker for content negotiation and caching.
4. Wire CI on tag: upload artifacts to R2, update `/latest`, publish metadata.
5. Add runbook and SLA notes (cache TTLs, rollback, DOI flow).


