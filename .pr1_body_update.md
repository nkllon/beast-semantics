- **feat(kiro): add cc-sdd-setup spec (requirements, design, tasks)**
- **refactor(spec): refine cc-sdd-setup spec - focus on verification and documentation**
- **docs(kiro): explicit command list; add verify script; README verification; approve spec for implementation**
- **release: add dual-hash freeze/verify, label; SBOM generation; README release docs**
- **release: dual-hash fixes; SBOM generator wiring; spec R5 immutability; update verify scripts**

<!-- CURSOR_SUMMARY -->
---

> [!NOTE]
> Introduces a new OSS RDF/SPARQL QA workflow and enhances CI with RDF/SPARQL/security gates, plus reuse decisioning tools and release freeze/verify with property-based tests.
> 
> - **CI/CD**:
>   - Extend `CI` workflow (`.github/workflows/ci.yml`) with Java/Node setup, SPARQL tools, Apache Jena RIOT validation, rdflint linting, SPARQL parse/format check, gitleaks secrets scan, and pip-audit; upload build artifacts.
>   - Add dedicated QA workflow (`.github/workflows/qa.yml`) for RDF/SPARQL validation, security scans, pip-audit gating (CVSS ≥7), domain metrics comparison, and job summary.
> - **Release & Integrity**:
>   - Add immutable release tooling: `tools/release-freeze.sh`, `tools/verify-release.sh`, `tools/release_freeze.py`, and `tools/generate-sbom.sh`; dual-hash manifests, labels, SBOM; PBTs for manifest integrity.
> - **Reuse Decisioning**:
>   - Add reuse policy (`.kiro/steering/policy.reuse.yml`) and tooling (`tools/reuse_index.mjs`, `tools/reuse_query.mjs`, `tools/reuse_decision.mjs`, `tools/reuse_evidence.mjs`, `tools/preflight.mjs`); generate evidence JSON under `.kiro/evidence/`.
> - **RDF/SPARQL Tooling**:
>   - Add local runner `tools/ci_local.sh`, Jena fetcher `tools/fetch_jena.sh`, RIOT Dockerfile, SPARQL checker (`tools/sparql_check.mjs`), RDFLib validator (`tools/validate_rdf.py`).
> - **Gateways**:
>   - Provide HyperGraphQL templates and GraphQL‑LD example docs under `tools/gateways/**`.
> - **Specs & Docs**:
>   - Add/refine specs under `.kiro/specs/**` (requirements, design, tasks, status) and update `README.md` with CC‑SDD verification, release freeze/verify, and CI gates.
> - **Tests**:
>   - Add Node-based property tests for CC‑SDD verification and release manifest integrity (`tools/cc-sdd/*.test.mjs`, `tools/publishing/paths.test.mjs`).
> - **Misc**:
>   - New `.gitignore`; update `package.json` (scripts/devDeps) and `requirements.txt`.
> 
> <sup>Written by [Cursor Bugbot](https://cursor.com/dashboard?tab=bugbot) for commit 34c2cb8d716e107d045efc10d2c064c28cdcde57. This will update automatically on new commits. Configure [here](https://cursor.com/dashboard?tab=bugbot).</sup>
<!-- /CURSOR_SUMMARY -->
## CI: GitHub App Governance updates

- Setup Java (Temurin 21) for Jena RIOT and rdflint
- RDF validation with RIOT over , , , 
- rdflint v1.1.13 lint step
- SPARQL parse/format checks via 
- gitleaks and pip-audit gates retained

Spec artifacts:
- 
- 
- 
- 

## CI: GitHub App Governance updates

- Setup Java (Temurin 21) for Jena RIOT and rdflint
- RDF validation with RIOT over `ontology/`, `shapes/`, `mappings/`, `build/`
- rdflint v1.1.13 lint step
- SPARQL parse/format checks via `tools/sparql_check.mjs`
- gitleaks and pip-audit gates retained

Spec artifacts:
- `.kiro/specs/github-app-governance/requirements.md`
- `.kiro/specs/github-app-governance/design.md`
- `.kiro/specs/github-app-governance/tasks.md`
- `.kiro/specs/github-app-governance/spec.json`
