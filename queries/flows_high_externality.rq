PREFIX lemon: <https://ns.nkllon.com/words/v1/>
SELECT ?flow ?externality
WHERE {
  ?flow a lemon:EnergyFlow ; lemon:externality ?externality .
  FILTER(?externality > 0.5)
}
ORDER BY DESC(?externality)
