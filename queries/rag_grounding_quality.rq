PREFIX lemon: <https://ns.nkllon.com/words/v1/>
SELECT ?answer ?faith ?relevance
WHERE {
  ?answer a lemon:Answer ;
          lemon:faithfulnessScore ?faith ;
          lemon:relevanceScore ?relevance .
}
ORDER BY DESC(?faith)
